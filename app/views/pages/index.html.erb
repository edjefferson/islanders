
<div class="row sea2">
  <p>Over <%=  number_with_delimiter(@castaways_count) %> castaways have been marooned on the BBC's desert island since <%= @start_date %>. But what records have they picked?</p>
  <p>Between them, they've picked <%=  number_with_delimiter(@records_count) %> discs by <%=  number_with_delimiter(@artists_count) %> artists.</p>
</div>

<div class="row beach1">
  <% @decades.each do |decade| %>
    <div class="col">
      <%= link_to decade, "?decade=#{decade}" %>
    </div>
  <% end %>
</div>

<div class="row beach2">
  <h2> <%= @current_decade %></h2>
</div>

<div class="row beach2">
  <div class="col">
    <h3>Top 10 <%= link_to "Artists", artists_path %></h3>
  </div>
  <div class="col">
    <h3>Top 10 <%= link_to "Discs", tracks_path %></h3>
  </div>
</div>

<div class="row">
  <div class="col">
    <table class = "table table-striped table-sm">
      <% @artists.each do |artist| %>
        <tr>
          <td><%= link_to artist["name"], "artists/" + artist["slug"] %></td>
          <td><%= artist["appearances"] %></td>
        </tr>
      <% end %>
    </table>
  </div>

  <div class="col">
    <table class = "table table-striped table-sm">
      <% @tracks.each do |track| %>
      <tr>
        <td><%= link_to track["name"], "tracks/" + track["slug"] %> - <%= track["artist"] %></td>

        <td><%= track["appearances"] %></td>
      </tr>
      <% end %>
    </table>
  </div>
</div>
<% if @books.count != 0 %>
  <div class="row beach2">
    <div class="col">
      <h3>Top Books</h3>
    </div>
    <div class="col">
      <h3>Top Luxuries</h3>
    </div>
  </div>

  <div class="row">
    <div class="col">
      <table class = "table table-striped table-sm">
        <% @books.each do |book| %>
          <tr>
            <td><%= book["name"] %></td>
            <td><%= book["appearances"] %></td>
          </tr>
        <% end %>
      </table>
    </div>

    <div class="col">
      <table class = "table table-striped table-sm">
        <% @luxuries.each do |luxury| %>
        <tr>
          <td><%= luxury["name"] %></td>
          <td><%= luxury["appearances"] %></td>
        </tr>
        <% end %>
      </table>
    </div>
  </div>
<% end %>
