
<div class="row sea2">
<p>Over <%=  number_with_delimiter(@castaways_count) %> castaways have been marooned on the BBC's desert island since <%= @start_date %>. But what records have they picked?</p>
</div>

<div class="row beach1">
  <div class="col">
    <h3>Top 10 <%= link_to "Artists", artists_path %></h3>

  </div>

<div class ="col">
  <h3>Top 10 <%= link_to "Tracks", tracks_path %></h3>
</div>
</div>


<div class= "row">
    <div class="col">
    <table class = "table table-striped table-sm">

    <% @artists.each do |artist| %>
    <tr>
      <td><%= link_to artist.name, artist %></td>
      <td><%= artist.appearances %></td>
    </tr>
    <% end %>
    </table>
  </div>

  <div class="col">


    <table class = "table table-striped table-sm">

    <% @tracks.each do |track| %>
    <tr>
      <td><%= link_to track.track, track %> - <%= track.artist.name %></td>

      <td><%= track.appearances %></td>
    </tr>
    <% end %>
    </table>

  </div>
</div>
