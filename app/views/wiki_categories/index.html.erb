<div class="row beach1">
  <% decades.values.each do |decade| %>
    <div class="col">
      <%= link_to decade, "?decade=#{decade}" %>
    </div>
  <% end %>
</div>

<div class = "row">

<div class ="col">
  <h2>Categories</h2>
<table class = "table table-striped table-sm">
<% @wiki_categories.each do |wiki_category| %>
<tr>
  <td><%= link_to wiki_category.name, wiki_category %></td>
  <td><%= wiki_category.count %></td>
</tr>
<% end %>
</table>
</div>
</div>
