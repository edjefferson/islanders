<div class = "row">

<div class ="col">
  <h2>Tracks </h2>
<table class = "table table-striped table-sm">
<% @tracks.each do |track| %>
<tr>
  <td><%= link_to track.artist.name + " - " + track.track, track %></td>
  <td><%= track.appearances %></td>
</tr>
<% end %>
</table>
</div>
</div>
